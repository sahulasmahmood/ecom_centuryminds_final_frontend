import { POSInterface } from "@/components/dashboard/pos/pos-interface";

export const metadata = {
  title: "POS | Skyspark Admin",
  description: "Offline Point of Sale System",
};

export default function POSPage() {
  return <POSInterface />;
}
